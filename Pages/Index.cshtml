@page
@model IndexModel
@{
    ViewData["Title"] = "Password Generator";
}

<div class="title-frame">Password Generator</div>

<form method="post">
    <div>
        <label>Password Length:</label>
        <input type="number" name="Length" value="16" min="4" max="32" class="digit-input" />
    </div>
    <div>
        <input type="checkbox" name="IncludeLetters" checked />
        <label>Include Letters</label>
    </div>
    <div>
        <input type="checkbox" name="IncludeDigits" checked />
        <label>Include Digits</label>
    </div>
    <div>
        <input type="checkbox" name="IncludeSpecial" checked />
        <label>Include Special Characters</label>
    </div>
    <button type="submit">Generate Password</button>
</form>

@if(Model.GeneratedPassword != null)
{
    <div class="result-frame">
        <strong>Your Password:</strong> <span class="generated-text">@Model.GeneratedPassword</span>
    </div>
}
